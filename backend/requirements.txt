# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0

# Base de datos
sqlalchemy>=2.0.0
asyncpg>=0.29.0          # driver async para PostgreSQL
psycopg2-binary>=2.9.9   # driver sync (para ETL / alembic)
geoalchemy2>=0.14.0

# Geoespacial (ETL GeoServer INYM)
geopandas>=0.14.0
shapely>=2.0.0
requests>=2.31.0

# Datos
pandas>=2.2.0
numpy>=1.26.0

# ML (instalación diferida hasta Fase 5)
# statsmodels>=0.14.0
# scikit-learn>=1.4.0

# Testing
pytest>=8.0.0
httpx>=0.27.0            # cliente async para tests de FastAPI
